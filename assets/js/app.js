const menuBtn=document.querySelector('.menu-btn');const navLinks=document.querySelector('.nav-links');if(menuBtn){menuBtn.addEventListener('click',()=>navLinks.classList.toggle('show'))}document.querySelectorAll('.estimate-form').forEach(form=>{form.addEventListener('submit',e=>{e.preventDefault();alert('Mission received. Call/Text (760) 906-1013 or email michaelallen918@gmail.com to finish connecting this form.')}});
